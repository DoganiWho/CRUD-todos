<div>
    <label>Title</label>
    <input type="text" name="title" value="<%= note.title %>">
</div>
<label>Tags</label>
<select name="tag">
    <!-- <% // tags.forEach(tag => { %>
        <% // if (tag.id === note.tag) { %>
            <option selected label="<%= // tag.name %>" value="<%= // tag.id %>">
        <% // } else { %>
            <option selected label="<%= // tag.name %>" value="<%= // tag.id %>">
        <% // } %>
    <% // }) %> -->
</select>
<!-- <input type="text" name="title" value="<%= // note.title %>"> -->
<div>
    <label>Description</label>
    <textarea type="text" name="description"> <%= note.description %></textarea>
</div>

<!-- TODO: implement Tags option to note (and todo) creation -->